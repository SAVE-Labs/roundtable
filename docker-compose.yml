services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: sqlite:/data/roundtable.db
      SERVER_NAME: Roundtable
    ports:
      - "1323:1323"
    volumes:
      - backend_data:/data

volumes:
  backend_data:
